<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <meta charset = 'utf-8'>
    
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/bootstrap/css/bootstrap.min.css' />
    <script src="/jquery/jquery.min.js"></script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
 

  </head>
  <body class ="container">
    <h1><%= title %>
      <small> - <%= user %>님 환영합니다</small>
    </h1>

    <div class = "row" id ="write">
      <div class = "col-md-3">
        
      </div>
      <div class= "col-md-8"></div>
      <div class ="col-md-1">
        <button type="button" class="btn btn-success" onclick="location.href='/write/f'">글 쓰기</button>
      </div>
    </div>
    <table class="table table-striped">
       <tr>
          <th>번호</th>
          <th>제목</th>
          <th>이름</th>
          <th>조회수</th>
       </tr>
       <!--웹에선 <(꺽쇠)% 많이 이용함-->
       <!--<%=datas%>-->
       <%
          for(i=0 ; i<datas.length ; i++){
             var data = datas[i];
             console.log(data.num);
       %>
          <tr>
             <td><%= data.num%></td>
             <td><a href="/read/<%=data.num%>"><%= data.title%></a></td>
             <td><%= data.name%></td>
             <td><%= data.hit%></td>
          </tr>
       <%
          }//for
         %>
    </table>
  </body>
</html>